//Este código cria um contexto em React utilizando a função createContext e o exporta para ser usado em outros componentes da aplicação.

import { createContext } from 'react';

const AppContext = createContext();

export default AppContext;
